spring:
  datasource:
    url: jdbc:mysql://localhost:3306/schooldb
    username: root
    password: Santosh@123
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      initialization-fail-timeout: 0
      connection-timeout: 5000
    
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8087

app:
  security:
    api-token: SchoolSecretToken123
    api-token-header: X-API-KEY
